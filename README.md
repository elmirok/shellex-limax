# Limax

Limax is the Shellex OS package manager app. The Shell `pack` command is its command-line front-end.

Current MVP: local `.sapp` install, installed package listing, package info and package removal.

## Shellex package

App id: `system.limax.local`

This repository is the private source package for the default Shellex OS package manager app.
